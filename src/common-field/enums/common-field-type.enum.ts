export enum CommonFieldType {
  text = 'text',
  email = 'email',
  password = 'password',
  date = 'date',
  datetime = 'datetime',
  number = 'number',
  boolean = 'boolean',
  select = 'select',
  file = 'file',
  checkbox = 'checkbox',
  radio = 'radio',
}
